export type UserConfig = {
    authToken: string;
    user?: {
        id: number;
        name: string;
        theme: string;
        imgUrl: string;
        email: string;
        password: string;
        authToken: string;
        createdAt: string;
        updatedAt: string;
    };
};
